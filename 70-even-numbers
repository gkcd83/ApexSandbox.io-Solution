public List<Integer> evenNumbers(Integer n) {
        List<Integer> lstInt = new List<Integer>();
        Integer sum=0;
        for (Integer i=0; i<n ;i++){
            sum += 2;
            lstInt.add(sum);
        }
        return lstInt.sort;

}